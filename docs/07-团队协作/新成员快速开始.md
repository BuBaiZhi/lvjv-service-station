# 新成员快速开始指南

**项目**：旅居服务站  
**仓库**：https://github.com/BuBaiZhi/lvji-service-station  
**创建日期**：2026-02-17

---

## 一、克隆仓库（首次）

### 1.1 复制仓库地址

```
https://github.com/BuBaiZhi/lvji-service-station.git
```

### 1.2 在本地克隆

打开 Git Bash，执行：

```bash
# 进入你的工作目录
cd d:\projects\  # 改为你的目录

# 克隆仓库
git clone https://github.com/BuBaiZhi/lvji-service-station.git

# 进入项目
cd lvji-service-station
```

### 1.3 配置 Git（只需一次）

```bash
# 配置用户信息（用你的 GitHub 账号）
git config user.name "你的名字"
git config user.email "你的GitHub邮箱@example.com"

# 配置行尾符自动转换
git config --global core.autocrlf true
```

---

## 二、开发流程

### 2.1 创建功能分支

```bash
# 1. 确保在 develop 分支
git checkout develop

# 2. 拉取最新代码
git pull origin develop

# 3. 基于 develop 创建自己的功能分支
git checkout -b feature/你的功能名

# 例如：
# git checkout -b feature/house-api
# git checkout -b feature/skill-module
# git checkout -b feature/square-post
```

### 2.2 分支命名规范

| 类型 | 示例 |
|------|------|
| 新功能 | feature/house-api |
| 修复bug | bugfix/login-bug |
| 优化 | refactor/order-logic |

### 2.3 开发代码

```bash
# 编辑文件...

# 多次提交（推荐经常提交）
git add .
git commit -m "feat: 添加民宿列表API"

git add .
git commit -m "feat: 添加分页功能"

git add .
git commit -m "fix: 修复参数验证"
```

### 2.4 同步最新代码（可选但推荐）

如果其他人已经提交了代码到 develop：

```bash
# 拉取最新 develop
git fetch origin develop

# 将最新的 develop 合并到你的分支
git merge origin/develop

# 或者用 rebase（推荐，保持线性历史）
git rebase origin/develop
```

---

## 三、提交 Pull Request

### 3.1 推送到远程

```bash
# 首次推送
git push -u origin feature/你的功能名

# 后续推送
git push origin feature/你的功能名
```

### 3.2 创建 PR（在 GitHub 网页）

1. 进入仓库 → **Pull requests** 标签
2. 点击 **New pull request**
3. 填写信息：
   - **base**: develop（目标分支）
   - **compare**: feature/你的功能名（你的分支）
4. **标题**示例：
   ```
   feat: 完成民宿列表 API 开发
   feat(house): 添加房源查询接口
   fix(order): 修复订单状态更新 bug
   ```
5. **描述**（可选，但推荐）：
   ```markdown
   ## 功能说明
   完成民宿模块的核心 API 开发
   
   ## 包含内容
   - [x] 民宿列表接口
   - [x] 分页和筛选
   - [x] 图片上传 OSS
   - [x] 缓存优化
   
   ## 测试
   - [x] 本地测试通过
   - [x] 没有 console 错误
   ```
6. 点击 **Create pull request**

### 3.3 等待 review

- 其他团队成员会 review 你的代码
- 如果有意见，在本地修改后重新 push（PR 会自动更新）
- 修改完成后，其他人点击 **Merge** 合并

---

## 四、合并后的操作

### 4.1 清理本地分支

```bash
# 切回 develop
git checkout develop

# 拉取最新代码
git pull origin develop

# 删除本地分支
git branch -d feature/你的功能名

# 删除远程分支
git push origin --delete feature/你的功能名
```

### 4.2 继续新的开发

```bash
# 重复"第二部分"的流程
git checkout -b feature/下一个功能
```

---

## 五、每日工作流程

**上午来了**：
```bash
# 切换到 develop 并拉取最新
git checkout develop
git pull origin develop

# 创建新分支（如果还没创建）
git checkout -b feature/house-pagination
```

**开发中间**：
```bash
# 多次提交
git add .
git commit -m "feat: 添加分页"

git add .
git commit -m "feat: 添加排序"
```

**下午完成**：
```bash
# 推送到远程
git push origin feature/house-pagination

# 在 GitHub 创建 PR
# 网页操作...
```

**PR 被 merge**：
```bash
# 本地同步和清理
git checkout develop
git pull origin develop
git branch -d feature/house-pagination
git push origin --delete feature/house-pagination
```

---

## 六、常见问题

### Q1：怎样查看所有分支？

```bash
# 本地分支
git branch

# 远程分支
git branch -r

# 全部分支
git branch -a
```

### Q2：分支间如何切换？

```bash
# 切换到 develop
git checkout develop

# 切换到你的 feature 分支
git checkout feature/你的功能名

# 创建并切换到新分支
git checkout -b feature/新功能
```

### Q3：不小心在错误的分支提交了怎么办？

```bash
# 查看最后的提交
git log --oneline -5

# 如果是最后一个，可以撤销
git reset --soft HEAD~1

# 或者用 cherry-pick 把提交复制到正确的分支
```

### Q4：代码冲突了怎么办？

```bash
# 1. 拉取最新 develop
git fetch origin develop

# 2. 尝试合并
git merge origin/develop

# 3. 如果有冲突，会显示 CONFLICT
# 手动编辑文件，保留需要的代码

# 4. 提交合并
git add .
git commit -m "fix: 解决合并冲突"

# 5. 推送
git push origin feature/你的功能名
```

### Q5：想查看 commit 历史？

```bash
# 简洁版本
git log --oneline -10

# 图形版本（推荐）
git log --oneline --graph --all

# 某个分支的历史
git log --oneline feature/你的功能名
```

---

## 七、commit message 规范

### 格式

```
<type>(<scope>): <subject>
```

### Type 类型

| 类型 | 说明 | 示例 |
|------|------|------|
| feat | 新功能 | feat(house): 添加民宿列表 API |
| fix | 修复 bug | fix(order): 修复订单状态更新 |
| docs | 文档 | docs: 更新 README |
| style | 代码格式 | style: 调整缩进 |
| refactor | 重构 | refactor: 优化用户服务 |
| perf | 性能 | perf: 减少 API 调用 |
| test | 测试 | test: 添加单元测试 |

### Scope（作用域）

常用的有：
```
user, house, skill, activity, square, message, order, api
```

### 完整示例

```bash
# 简单
git commit -m "feat(house): 添加民宿列表 API"

# 详细
git commit -m "feat(house): 添加民宿列表 API

- 支持分页和筛选
- 集成 OSS 文件存储
- 添加缓存优化"
```

---

## 八、快速参考命令

```bash
# 克隆仓库
git clone https://github.com/BuBaiZhi/lvji-service-station.git

# 创建功能分支
git checkout -b feature/功能名

# 提交代码
git add .
git commit -m "feat: 功能描述"
git push origin feature/功能名

# 拉取最新
git pull origin develop

# 查看状态
git status

# 查看历史
git log --oneline -10

# 切换分支
git checkout develop

# 删除分支
git branch -d feature/功能名
```

---

## 九、需要帮助？

- 📖 详细文档：`docs/07-团队协作/GitHub团队协作指南.md`
- 🔍 API 设计：`docs/06-API设计/API与数据库设计文档.md`
- 💬 有问题？在 GitHub Issues 中讨论

---

**祝你开发愉快！🚀**
