# 旅居服务站后端 - 自建后端开发完成

**更新时间**: 2026-03-01
**版本号**: v1.5.0
**项目状态**: 自建后端核心模块开发完成

---

## 一、本次更新概述

本次更新完成了自建后端的核心模块开发，包括基础设施、认证、用户、订单、消息五大模块，共计24个API接口，35个测试用例全部通过。

---

## 二、技术架构

### 2.1 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | v24.x | 运行环境 |
| Express | 4.18.2 | Web框架 |
| MySQL2 | 3.6.0 | 数据库 |
| JWT | 9.0.2 | 认证 |
| Multer | 2.1.0 | 文件上传 |

### 2.2 架构设计

```
┌─────────────────────────────────────────────┐
│           前端（微信小程序）                │
└──────────────────┬──────────────────────────┘
                   │ HTTPS + JSON
┌──────────────────▼──────────────────────────┐
│         Express 应用（Node.js）              │
├─────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  中间件  │  │  路由层  │  │ 控制器层 │  │
│  ├──────────┤  ├──────────┤  ├──────────┤  │
│  │ - auth   │→ │ - auth   │→ │ - auth   │  │
│  │ - logger │  │ - user   │  │ - user   │  │
│  │ - error  │  │ - order  │  │ - order  │  │
│  │ - limit  │  │ - message│  │ - message│  │
│  │ - cors   │  │          │  │          │  │
│  └──────────┘  └──────────┘  └─────┬────┘  │
│                                     │       │
│                            ┌────────▼────┐  │
│                            │   模型层    │  │
│                            └────────┬────┘  │
└─────────────────────────────────────┼───────┘
                                      │
                         ┌────────────▼────────────┐
                         │   MySQL 数据库          │
                         │  12张数据表             │
                         └─────────────────────────┘
```

### 2.3 双通道架构

```
┌─────────────────────────────────────────────────────────────┐
│                    apiProxy.js（统一代理层）                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  判断逻辑：有 Token？                                  │    │
│  │    有 Token → HTTP后端（自建）                        │    │
│  │    无 Token → 云开发（微信云数据库）                   │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────┬──────────────────────────┬───────────────────┘
               │                          │
               ▼                          ▼
┌──────────────────────────┐   ┌──────────────────────────┐
│     自建后端             │   │     云开发               │
│  (localhost:3000)        │   │  (微信云数据库)          │
├──────────────────────────┤   ├──────────────────────────┤
│ ✅ 认证（登录/刷新Token） │   │ ✅ 民宿列表/发布         │
│ ✅ 用户信息管理          │   │ ✅ 技能列表/发布         │
│ ✅ 订单管理              │   │ ✅ 广场帖子列表/发布     │
│ ✅ 消息通知              │   │ ✅ 评论/点赞/收藏       │
│ ✅ 复杂报表/统计         │   │ ✅ 浏览历史             │
└──────────────────────────┘   └──────────────────────────┘
```

---

## 三、已实现模块

### 3.1 基础设施模块

| 文件 | 功能 | 状态 |
|------|------|------|
| `utils/logger.js` | 日志系统（DEBUG/INFO/WARN/ERROR） | ✅ |
| `middleware/validate.js` | 参数验证（20+规则） | ✅ |
| `middleware/errorHandler.js` | 统一错误处理 | ✅ |
| `middleware/rateLimit.js` | 限流中间件 | ✅ |
| `services/uploadService.js` | 文件上传服务 | ✅ |
| `utils/errors.js` | 自定义错误类 | ✅ |

### 3.2 认证模块

| 接口 | 方法 | 功能 | 测试 |
|------|------|------|------|
| `/api/auth/login` | POST | 微信登录 | ✅ |
| `/api/auth/refresh` | POST | 刷新Token | ✅ |

### 3.3 用户模块

| 接口 | 方法 | 功能 | 测试 |
|------|------|------|------|
| `/api/user/me` | GET | 获取当前用户 | ✅ |
| `/api/user/stats` | GET | 统计数据 | ✅ |
| `/api/user/search` | GET | 搜索用户 | ✅ |
| `/api/user/:userId` | GET | 公开信息 | ✅ |
| `/api/user/info` | PUT | 更新信息 | ✅ |
| `/api/user/identity` | POST | 设置身份 | ✅ |
| `/api/user/settings` | PUT | 更新设置 | ✅ |
| `/api/user/avatar` | POST | 上传头像 | ✅ |

### 3.4 订单模块

| 接口 | 方法 | 功能 | 测试 |
|------|------|------|------|
| `/api/order/list` | GET | 订单列表 | ✅ |
| `/api/order/stats` | GET | 订单统计 | ✅ |
| `/api/order/:id` | GET | 订单详情 | ✅ |
| `/api/order` | POST | 创建订单 | ✅ |
| `/api/order/:id/cancel` | PUT | 取消订单 | ✅ |
| `/api/order/:id/confirm` | PUT | 确认订单 | ✅ |
| `/api/order/:id/complete` | PUT | 完成订单 | ✅ |

### 3.5 消息模块

| 接口 | 方法 | 功能 | 测试 |
|------|------|------|------|
| `/api/message/list` | GET | 消息列表 | ✅ |
| `/api/message/unread` | GET | 未读数量 | ✅ |
| `/api/message/:id` | GET | 消息详情 | ✅ |
| `/api/message/:id/read` | PUT | 标记已读 | ✅ |
| `/api/message/read-all` | PUT | 全部已读 | ✅ |
| `/api/message/:id` | DELETE | 删除消息 | ✅ |
| `/api/message/clear-read` | DELETE | 清空已读 | ✅ |

---

## 四、数据库设计

### 4.1 数据表（12张）

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `users` | 用户表 | openid, nickname, avatar, identity |
| `items` | 内容物品表 | type, title, description, price |
| `orders` | 订单表 | order_no, user_id, seller_id, status |
| `messages` | 消息通知表 | user_id, type, title, content |
| `chat_messages` | 聊天消息表 | from_id, to_id, content |
| `favorites` | 收藏表 | user_id, item_id |
| `browse_history` | 浏览历史表 | user_id, item_id |
| `likes` | 点赞表 | user_id, item_id |
| `comments` | 评论表 | user_id, item_id, content |
| `follows` | 关注表 | follower_id, following_id |
| `reviews` | 评价表 | order_id, rating, content |
| `drafts` | 草稿表 | user_id, type, content |

### 4.2 索引优化

- 用户表：openid索引、身份索引
- 订单表：用户ID索引、卖家ID索引、状态索引
- 消息表：用户ID索引、类型索引、已读索引

---

## 五、测试报告

### 5.1 测试结果

```
┌─────────────────────────────────────────────────────────┐
│                    后端测试报告                          │
├─────────────────────────────────────────────────────────┤
│  基础API测试     ✅ 通过 13 / 失败 0                    │
│  集成测试        ✅ 通过 9 / 失败 0                     │
│  订单模块测试    ✅ 通过 8 / 失败 0                     │
│  消息模块测试    ✅ 通过 5 / 失败 0                     │
├─────────────────────────────────────────────────────────┤
│  总计           ✅ 通过 35 / 失败 0                     │
└─────────────────────────────────────────────────────────┘
```

### 5.2 测试覆盖

- ✅ 健康检查
- ✅ 参数验证
- ✅ Token认证
- ✅ 权限控制
- ✅ 业务逻辑
- ✅ 404处理
- ✅ 限流机制

---

## 六、项目结构

```
backend/
├── src/
│   ├── app.js                    # 主入口
│   ├── config/
│   │   └── database.js           # 数据库配置
│   ├── controllers/
│   │   ├── authController.js     # 认证控制器
│   │   ├── userController.js     # 用户控制器
│   │   ├── orderController.js    # 订单控制器
│   │   └── messageController.js  # 消息控制器
│   ├── middleware/
│   │   ├── auth.js               # JWT认证
│   │   ├── errorHandler.js       # 错误处理
│   │   ├── rateLimit.js          # 限流
│   │   └── validate.js           # 参数验证
│   ├── models/
│   │   ├── user.js               # 用户模型
│   │   ├── order.js              # 订单模型
│   │   └── message.js            # 消息模型
│   ├── routes/
│   │   ├── auth.js               # 认证路由
│   │   ├── user.js               # 用户路由
│   │   ├── order.js              # 订单路由
│   │   └── message.js            # 消息路由
│   ├── services/
│   │   ├── uploadService.js      # 上传服务
│   │   └── wechatService.js      # 微信服务
│   └── utils/
│       ├── errors.js             # 错误类
│       └── logger.js             # 日志工具
├── test/
│   ├── api.test.js               # 基础测试
│   ├── integration.test.js       # 集成测试
│   ├── order.test.js             # 订单测试
│   └── message.test.js           # 消息测试
├── database.sql                  # 数据库脚本
├── package.json
└── .env                          # 环境配置
```

---

## 七、Git提交记录

```
ad60112 feat(backend): 消息模块开发完成
0d32d13 feat(backend): 订单模块开发完成
b5ee55c test(backend): 添加API测试脚本
0fbbf12 feat(backend): 阶段2 - 用户模块开发
7b9646d fix: 修复草稿服务，支持本地存储降级
```

---

## 八、下一步计划

1. ✅ ~~自建后端核心模块开发~~
2. 🔲 云开发集合完善
3. 🔲 前后端联调
4. 🔲 部署上线

---

**文档版本**: v1.5.0
**下次更新**: 云开发完成后
