/* 编辑资料页面样式 */
.page {
  min-height: 100vh;
  background-color: #f5f5f5;
  padding-bottom: 30rpx;
}

/* 用户信息卡片 */
.user-card {
  position: relative;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 40rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
  overflow: hidden;
}

.user-card-bg {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  z-index: 0;
}

.user-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 6rpx solid rgba(255, 255, 255, 0.5);
  position: relative;
  z-index: 1;
}

.user-info {
  flex: 1;
  min-width: 0;
  position: relative;
  z-index: 1;
}

/* 更换背景按钮 */
.change-bg-btn {
  position: absolute;
  bottom: 20rpx;
  right: 20rpx;
  z-index: 2;
  padding: 12rpx 24rpx;
  background: rgba(0, 0, 0, 0.5);
  border-radius: 30rpx;
}

.change-bg-btn text {
  font-size: 24rpx;
  color: #fff;
}

.user-name-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.user-nickname {
  font-size: 36rpx;
  font-weight: 600;
  color: #fff;
}

.identity-badge {
  padding: 4rpx 16rpx;
  background: rgba(255, 255, 255, 0.25);
  border-radius: 20rpx;
  font-size: 22rpx;
  color: #fff;
}

.user-meta-row {
  display: flex;
  align-items: center;
  gap: 4rpx;
}

.user-gender,
.user-age,
.user-id {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.85);
}

/* 表单区域 */
.form-section {
  padding: 20rpx;
}

/* 输入框 */
.input {
  width: 100%;
  height: 88rpx;
  border: 1rpx solid #e0e0e0;
  border-radius: 12rpx;
  padding: 0 24rpx;
  font-size: 30rpx;
}

.input:focus {
  border-color: #1890ff;
}

.form-item {
  background: #fff;
  margin-bottom: 20rpx;
  padding: 30rpx;
  border-radius: 12rpx;
}

.label {
  display: block;
  font-size: 30rpx;
  color: #333;
  margin-bottom: 20rpx;
  font-weight: 500;
}

/* 头像 */
.avatar-wrapper,
.avatar-edit-wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.avatar-large {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
}

.change-tip {
  font-size: 26rpx;
  color: #1890ff;
}

/* 输入框 */
.input {
  width: 100%;
  height: 44px;
  border: 1px solid #e0e0e0;
  border-radius: 8rpx;
  padding: 0 20rpx;
  font-size: 15px;
}

.input:focus {
  border-color: #1890ff;
}

/* 性别选择器 */
.gender-picker {
  display: flex;
  gap: 20rpx;
}

.gender-item {
  flex: 1;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid #e0e0e0;
  border-radius: 8rpx;
  font-size: 15px;
  color: #666;
  transition: all 0.3s;
}

.gender-item.active {
  border-color: #1890ff;
  background-color: #e6f7ff;
  color: #1890ff;
}

/* 文本域 */
.textarea {
  width: 100%;
  min-height: 120rpx;
  border: 1px solid #e0e0e0;
  border-radius: 8rpx;
  padding: 20rpx;
  font-size: 15px;
}

.textarea:focus {
  border-color: #1890ff;
}

.char-count {
  text-align: right;
  font-size: 12px;
  color: #999;
  margin-top: 10rpx;
}

/* 按钮 */
.btn-wrapper {
  margin-top: 40rpx;
}

/* 深色模式适配 */
.dark-mode .page {
  background-color: #1a1a1a;
}

.dark-mode .form-item {
  background: #2a2a2a;
}

.dark-mode .label {
  color: #f0f0f0;
}

.dark-mode .input,
.dark-mode .textarea {
  background: #333;
  border-color: #444;
  color: #f0f0f0;
}

.dark-mode .gender-item {
  background: #333;
  border-color: #444;
  color: #ccc;
}

.dark-mode .gender-item.active {
  background: #1a3a5a;
  border-color: #1890ff;
}
