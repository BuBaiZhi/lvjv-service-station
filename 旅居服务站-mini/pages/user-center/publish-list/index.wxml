<!-- 我的发布页面 -->
<view class="page {{theme === 'dark' ? 'dark-mode' : ''}} {{appVersion === 'elderly' ? 'elderly-mode' : ''}}">
  <!-- 导航栏 -->
  <view class="page-header">
    <view class="back-btn" bindtap="goBack">返回</view>
    <text class="page-title">我的发布</text>
    <view class="add-icon" bindtap="addPublish">+</view>
  </view>

  <!-- 标签栏 -->
  <view class="tabs">
    <view 
      class="tab-item {{activeTab === 'all' ? 'active' : ''}}"
      bindtap="switchTab"
      data-tab="all"
    >
      <text>全部</text>
    </view>
    <view 
      class="tab-item {{activeTab === 'active' ? 'active' : ''}}"
      bindtap="switchTab"
      data-tab="active"
    >
      <text>进行中</text>
    </view>
    <view 
      class="tab-item {{activeTab === 'ended' ? 'active' : ''}}"
      bindtap="switchTab"
      data-tab="ended"
    >
      <text>已结束</text>
    </view>
  </view>

  <!-- 发布列表 -->
  <view class="content">
    <view 
      class="publish-card"
      wx:for="{{publishList}}"
      wx:key="id"
    >
      <image class="publish-image" src="{{item.image || 'https://picsum.photos/400/300?random=' + item.id}}" mode="aspectFill"></image>
      
      <view class="publish-info">
        <view class="publish-header">
          <text class="publish-title">{{item.title}}</text>
          <text class="publish-status {{item.status}}">{{item.statusText}}</text>
        </view>

        <view class="publish-meta">
          <text class="meta-item">👁 {{item.views}} 浏览</text>
          <text class="meta-item">❤️ {{item.likes}} 喜欢</text>
          <text class="meta-item">📅 {{item.date}}</text>
        </view>

        <view class="publish-actions">
          <button 
            class="action-btn" 
            size="mini"
            bindtap="editPublish"
            data-id="{{item.id}}"
          >
            编辑
          </button>
          <button 
            class="action-btn delete" 
            size="mini"
            bindtap="deletePublish"
            data-id="{{item.id}}"
          >
            删除
          </button>
        </view>
      </view>
    </view>

    <!-- 空状态 -->
    <view class="empty" wx:if="{{publishList.length === 0}}">
      <text class="empty-icon">📝</text>
      <text class="empty-text">还没有发布内容</text>
      <button class="btn-primary" bindtap="addPublish">立即发布</button>
    </view>
  </view>
</view>
